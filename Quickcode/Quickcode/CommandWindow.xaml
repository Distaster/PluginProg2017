<Window x:Class="Quickcode.CommandWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quickcode"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="100" Width="100" Height="350">
    <Grid Background="#FF1E1E1E">
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Text="Konstrukte" Foreground="White"/>
        <ListBox x:Name="konstruke" Height="246" Margin="0,31,0,0" VerticalAlignment="Top" Background="#FF1E1E1E" Foreground="White">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Path=Name}"></TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button x:Name="insertC" Content="Einfügen" HorizontalAlignment="Left" Margin="0,284,0,0" VerticalAlignment="Top" Width="92" Click="Click" Height="25"/>


    </Grid>
</Window>